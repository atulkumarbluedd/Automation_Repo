package com.group.guru99;

import java.util.Iterator;
import java.util.PriorityQueue;

public class priorityquee {
public static void main(String[] args) {
	PriorityQueue<student> pq= new  PriorityQueue<>();
	pq.add(new student(2,"rohan"));
	pq.add(new student(3,"sohan"));
	pq.add(new student(0,"naresh"));
	
	Iterator<student> itr = pq.iterator();
	System.out.println(itr);
	while( itr.hasNext())
	{
		student st=itr.next();
//		System.out.println(st.poll().toString());
	}
}
}
